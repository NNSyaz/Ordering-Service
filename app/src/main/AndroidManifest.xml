<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.robotemi.permission.FACE_RECOGNITION" />
    <uses-permission android:name="com.robotemi.permission.MAP" />
    <uses-permission android:name="com.robotemi.permission.LOCATIONS" />
    <uses-permission android:name="com.robotemi.permission.SEQUENCE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TemiRestaurantApp"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.TemiRestaurantApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".NavigationActivity"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.TemiRestaurantApp.NoActionBar" />

        <activity
            android:name=".OrderingActivity"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.TemiRestaurantApp.NoActionBar" />

        <meta-data
            android:name="com.robotemi.sdk.metadata.SKILL"
            android:value="@string/app_name" />

        <meta-data
            android:name="com.robotemi.sdk.metadata.WELCOME_SENTENCE"
            android:value="Welcome to our restaurant! I'm here to help you." />
    </application>
</manifest>
